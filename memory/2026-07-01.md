# Memory Log: July 1, 2026

- **Leaflet Map Generator Project Built**: Created a lightweight interactive Leaflet map generator from WordPress REST API inputs.
- **Port Reconfigured**: Server modified to run on port `5050` instead of port `5000` to avoid conflicts with macOS `ControlCenter` (AirPlay Receiver).
- **Vite Output Configured**: Admin dashboard Vite configuration updated to use `/admin/` base routing and output build products to `../public/admin` for single-port unified deployment.
- **Mock Feed Integrated**: Created `/api/mock-wp` WordPress emulator to simplify local development, testing clustering (deduplication) logic, and rendering visuals in previews.
- **Strict shadcn/ui Refactoring**: Integrated custom shadcn UI components (`Button`, `Card`, `Tabs`, `Table`, `Input`, `Label`, `Checkbox`, `Alert`) built on top of Radix UI primitives.
- **Theme Restructured**: Modified style variables to reflect a strictly minimal, neutral-color design with a dark/light system and clean black accent borders/active states.
- **Border Style Bug Resolved**: Removed invalid HSL wrapper overrides on OKLCH variables from index.css. This allows Native OKLCH styling from shadcn v4 to render correctly, fixing the default thick black borders.
- **Official Path Aliases Integrated**: Updated the dashboard App.tsx and all component UI files to import modules using the path alias schema (`@/components/ui/*` and `@/lib/utils`) instead of relative paths, satisfying the requirement to import and use the actual library components.
- **CVA Standard Integration**: Rebuilt all component files using the standard `class-variance-authority` (CVA) variant management library and slot properties, aligning them exactly with official shadcn/ui upstream registries and avoiding custom styles.
- **Default Fonts & Layout Styling Restored**: Removed custom Google Font (Inter) imports and style overrides in index.html, allowing the default Geist Sans font to render natively without styling overrides.
- **Monospace Font Stripped**: Cleaned up all layout classes in App.tsx containing `font-mono` to ensure the entire page layout falls back natively to shadcn's default Geist Sans font family without monospace text.
- **Verification Complete**: Successfully tested curl sync actions, settings updates, and markers.json structure validation. All modules compile without errors.
